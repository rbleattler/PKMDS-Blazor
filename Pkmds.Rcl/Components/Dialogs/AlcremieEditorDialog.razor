@inherits BasePkmdsComponent

<MudDialog>
    <TitleContent>
        <MudText Typo="@Typo.h6">Alcremie Appearance</MudText>
    </TitleContent>
    <DialogContent>
        @if (Pokemon is not null)
        {
            <MudText Typo="@Typo.subtitle2"
                     Class="mb-1">Cream
            </MudText>
            <MudStack Row
                      Wrap="@Wrap.Wrap"
                      Spacing="1"
                      Class="mb-3">
                @for (byte i = 0; i < CreamNames.Length; i++)
                {
                    var creamIdx = i;
                    var isSelected = creamIdx == selectedCream;
                    <MudTooltip Text="@CreamNames[creamIdx]">
                        <MudPaper Outlined="@(!isSelected)"
                                  Elevation="@(isSelected
                                                 ? 4
                                                 : 0)"
                                  Class="@($"pa-1 cursor-pointer{(isSelected ? " mud-border-primary" : "")}")"
                                  Style="@(isSelected
                                             ? "border: 2px solid var(--mud-palette-primary);"
                                             : "border: 2px solid transparent;")"
                                  @onclick="@(() => SelectCream(creamIdx))">
                            <MudImage Src="@ImageHelper.GetPokemonSpriteFilenameForForm((ushort)Species.Alcremie, Pokemon.Context, creamIdx, selectedDeco)"
                                      Alt="@CreamNames[creamIdx]"
                                      ObjectFit="@ObjectFit.Contain"
                                      Width="48"
                                      Height="48"/>
                        </MudPaper>
                    </MudTooltip>
                }
            </MudStack>

            <MudText Typo="@Typo.subtitle2"
                     Class="mb-1">Decoration
            </MudText>
            <MudStack Row
                      Wrap="@Wrap.Wrap"
                      Spacing="1"
                      Class="mb-3">
                @for (uint i = 0; i < DecorationNames.Length; i++)
                {
                    var decoIdx = i;
                    var isSelected = decoIdx == selectedDeco;
                    <MudTooltip Text="@DecorationNames[decoIdx]">
                        <MudPaper Outlined="@(!isSelected)"
                                  Elevation="@(isSelected
                                                 ? 4
                                                 : 0)"
                                  Class="@($"pa-1 cursor-pointer{(isSelected ? " mud-border-primary" : "")}")"
                                  Style="@(isSelected
                                             ? "border: 2px solid var(--mud-palette-primary);"
                                             : "border: 2px solid transparent;")"
                                  @onclick="@(() => SelectDeco(decoIdx))">
                            <MudImage Src="@ImageHelper.GetPokemonSpriteFilenameForForm((ushort)Species.Alcremie, Pokemon.Context, selectedCream, decoIdx)"
                                      Alt="@DecorationNames[decoIdx]"
                                      ObjectFit="@ObjectFit.Contain"
                                      Width="48"
                                      Height="48"/>
                        </MudPaper>
                    </MudTooltip>
                }
            </MudStack>

            <MudStack AlignItems="@AlignItems.Center">
                <MudText Typo="@Typo.caption">Preview</MudText>
                <MudImage Src="@ImageHelper.GetPokemonSpriteFilenameForForm((ushort)Species.Alcremie, Pokemon.Context, selectedCream, selectedDeco)"
                          Alt="Alcremie preview"
                          ObjectFit="@ObjectFit.Contain"
                          Width="96"
                          Height="96"/>
                <MudText Typo="@Typo.body2">@CreamNames[selectedCream] + @DecorationNames[selectedDeco]</MudText>
            </MudStack>
        }
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="@Cancel"
                   Variant="@Variant.Outlined">Cancel
        </MudButton>
        <MudButton OnClick="@Confirm"
                   Variant="@Variant.Filled"
                   Color="@Color.Primary">Confirm
        </MudButton>
    </DialogActions>
</MudDialog>
